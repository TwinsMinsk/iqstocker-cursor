# Проверка настройки Tariff Limits в Supabase

## ✅ Что уже сделано:

1. ✅ **Таблица создана в Supabase** - вы выполнили SQL в Dashboard
2. ✅ **TariffService настроен** - читает из базы данных с fallback на settings.py
3. ✅ **Админ-панель настроена** - может редактировать тарифы через `/tariff-limits`
4. ✅ **Все handlers обновлены** - используют TariffService вместо прямого чтения из settings

## 🔍 Как проверить, что всё работает:

### 1. Проверка через Supabase Dashboard:

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдите в **Table Editor** → `tariff_limits`
3. Должны быть 4 записи:
   - FREE: Analytics=0, Themes=4
   - TEST_PRO: Analytics=1, Themes=2, Duration=14
   - PRO: Analytics=1, Themes=4
   - ULTRA: Analytics=2, Themes=4

### 2. Проверка через админ-панель (локально):

1. Запустите админ-панель:
   ```powershell
   uvicorn admin_panel.main:app --host 0.0.0.0 --port 8000 --reload
   ```

2. Откройте http://localhost:8000/tariff-limits
3. Должны отображаться все тарифы с текущими значениями из базы
4. Попробуйте изменить значение и сохранить
5. Проверьте в Supabase Dashboard - изменения должны быть видны

### 3. Проверка через бота:

1. Запустите бота локально
2. Проверьте создание нового пользователя - лимиты должны браться из базы
3. Проверьте профиль пользователя - лимиты должны отображаться корректно

### 4. Проверка на Railway:

После деплоя на Railway:
1. Измените тариф через админ-панель (если она доступна)
2. Проверьте в боте - изменения должны применяться сразу
3. Все изменения сохраняются в Supabase и доступны везде

## 📋 Как работает система:

### Локальная разработка:
- Бот → читает из Supabase через `TariffService`
- Админ-панель → читает и сохраняет в Supabase через `TariffService`
- Изменения сохраняются в Supabase и доступны сразу

### Railway (Production):
- Бот → читает из Supabase через `TariffService`
- Изменения из админ-панели сохраняются в Supabase
- Все используют одну и ту же базу данных (Supabase)

### Источник данных:
1. **Первичный источник**: Таблица `tariff_limits` в Supabase
2. **Fallback**: Если данных нет в базе → используется `settings.py`

## 🎯 Важные моменты:

1. **Все изменения сохраняются в Supabase** - одна база для локальной разработки и Railway
2. **Админ-панель работает с Supabase** - изменения применяются сразу
3. **Бот использует данные из базы** - при создании пользователей, обновлении подписок и т.д.
4. **Fallback на settings.py** - если база недоступна, используются значения по умолчанию

## 🚀 Следующие шаги:

1. ✅ Таблица создана в Supabase
2. ✅ Проверьте через админ-панель, что данные читаются
3. ✅ Попробуйте изменить тариф через админ-панель
4. ✅ Проверьте, что изменения сохраняются в Supabase
5. ✅ После деплоя на Railway - всё будет работать автоматически

## ⚠️ Если что-то не работает:

1. **Проверьте DATABASE_URL** - должен указывать на Supabase
2. **Проверьте таблицу в Supabase** - должна существовать и содержать данные
3. **Проверьте логи бота** - должны быть сообщения о чтении из базы
4. **Проверьте админ-панель** - должна показывать данные из базы

## 📝 Полезные команды:

```powershell
# Проверка через Supabase CLI (если таблица существует)
npx supabase db dump --schema public --table tariff_limits --data-only

# Проверка через Python (если подключение работает)
python scripts/verify_tariff_limits.py
```

