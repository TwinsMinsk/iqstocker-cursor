# 🔧 Исправление конфликта зависимостей httpx

## 🔴 Проблема

При деплое на Railway возникла ошибка:

```
ERROR: Cannot install -r requirements.txt (line 22), -r requirements.txt (line 23), -r requirements.txt (line 3), -r requirements.txt (line 66) and httpx==0.28.1 because these package versions have conflicting dependencies.

The conflict is caused by:
    The user requested httpx==0.28.1
    python-telegram-bot 21.7 depends on httpx~=0.27
    openai 1.54.4 depends on httpx<1 and >=0.23.0
    anthropic 0.34.2 depends on httpx<1 and >=0.23.0
    supabase 2.3.4 depends on httpx<0.26 and >=0.24
```

**Суть конфликта:**
- `httpx==0.28.1` указан в requirements.txt
- `python-telegram-bot 21.7` требует `httpx~=0.27` (то есть 0.27.x)
- `supabase 2.3.4` требует `httpx<0.26 and >=0.24` (то есть 0.24.x или 0.25.x)

Эти требования **несовместимы**!

---

## ✅ Решение

### Вариант 1: Обновить supabase (реализовано)

Обновлён `supabase` до версии `>=2.8.0`, которая должна поддерживать `httpx 0.27+`:

```txt
httpx>=0.27,<0.28
supabase>=2.8.0
```

**Преимущества:**
- ✅ Совместимо с `python-telegram-bot 21.7`
- ✅ Использует более новую версию supabase (лучшая производительность, новые фичи)

**Недостатки:**
- ⚠️ Требует проверки, что supabase 2.8.0+ существует и совместим с текущим кодом

---

### Вариант 2: Если supabase 2.8.0+ не существует

Если обновление supabase не работает, можно:

1. **Удалить `python-telegram-bot`** (если не используется в коде):
   ```txt
   # python-telegram-bot==21.7  # Удалено, если не используется
   ```

2. **Или использовать `httpx 0.25.x` и обновить `python-telegram-bot`**:
   ```txt
   httpx>=0.25,<0.26
   python-telegram-bot>=21.8  # Если версия существует
   ```

---

## 🧪 Проверка после исправления

После деплоя проверь:

1. **Логи Railway Build:**
   - Должно быть: `Successfully installed httpx-0.27.x supabase-2.x.x`
   - НЕ должно быть: `ERROR: ResolutionImpossible`

2. **Импорты в коде:**
   - Проверь, что `from supabase import ...` работает
   - Проверь, что все функции Supabase Storage работают

3. **Тест CSV обработки:**
   - Загрузи тестовый CSV
   - Проверь, что файл сохраняется в Supabase Storage
   - Проверь, что отчёт генерируется корректно

---

## 📝 Изменения в requirements.txt

### До:
```txt
httpx==0.28.1
supabase==2.3.4
```

### После:
```txt
httpx>=0.27,<0.28
supabase>=2.8.0
```

---

## 🔍 Альтернативные решения (если проблема остаётся)

### Решение A: Удалить python-telegram-bot

Если `python-telegram-bot` не используется в коде (используется только `aiogram`), можно удалить:

```txt
# Telegram Bot Framework
aiogram==3.13.1
# python-telegram-bot==21.7  # Удалено, не используется
```

Затем использовать:
```txt
httpx>=0.24,<0.26
supabase==2.3.4
```

### Решение B: Использовать старую версию httpx

Если нужно сохранить `supabase 2.3.4`:

```txt
httpx>=0.25,<0.26
supabase==2.3.4
# python-telegram-bot нужно обновить до версии, поддерживающей httpx 0.25
```

---

## 🎯 Рекомендация

**Используй Вариант 1** (обновление supabase), так как:
- ✅ Использует более новую версию библиотеки
- ✅ Совместимо со всеми зависимостями
- ✅ Не требует удаления функциональности

Если после деплоя возникнут проблемы с Supabase API, можно откатиться к Варианту 2.

---

**Дата создания**: 13.11.2025  
**Версия**: 1.0  
**Статус**: Готово к тестированию

