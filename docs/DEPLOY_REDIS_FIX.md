# 🚀 Деплой исправлений Redis на Railway

## 📋 Что было исправлено

### 1. **`workers/actors.py`**
Добавлены Railway-compatible параметры для Dramatiq RedisBroker:
- `socket_connect_timeout=10` — увеличен таймаут для Railway Proxy
- `socket_keepalive=True` — включён TCP keepalive
- `retry_on_timeout=True` — автоматический retry при таймауте
- `health_check_interval=30` — проверка здоровья соединения каждые 30 секунд

### 2. **`config/database.py`**
Добавлены те же параметры для Redis client в боте (для кэширования user/limits/lexicon)

### 3. **`core/cache/user_cache.py`**
Добавлена защита от ошибок Redis (graceful degradation):
- Все методы проверяют `if redis_client is None` перед использованием
- Бот продолжит работать даже если Redis недоступен

---

## 🎯 Инструкция по деплою

### Шаг 1: Commit изменений

```bash
# Убедись, что ты в правильной ветке
git status

# Добавь изменённые файлы
git add workers/actors.py
git add config/database.py
git add core/cache/user_cache.py
git add docs/LOGS_ANALYSIS_SUMMARY.md
git add docs/DEPLOY_REDIS_FIX.md
git add docs/REDIS_FIX_GUIDE.md

# Commit с понятным сообщением
git commit -m "fix: Add Railway-compatible Redis connection settings

- Add socket keepalive and timeout settings for Railway Redis Proxy
- Add graceful degradation for Redis cache operations
- Improve Dramatiq worker Redis connection stability"
```

### Шаг 2: Push на Railway

```bash
# Push в твою ветку (замени <branch_name> на имя твоей ветки)
git push origin <branch_name>
```

**Railway автоматически:**
1. Обнаружит новый commit
2. Соберёт Docker образы
3. Перезапустит все сервисы (bot, worker, web)

---

## ⏱️ Время деплоя

- **Build**: ~2-3 минуты
- **Deploy**: ~1-2 минуты
- **Всего**: ~3-5 минут

---

## ✅ Проверка после деплоя

### 1. Проверь логи Worker Service

Railway Dashboard → Worker service → Logs

**Должно появиться:**
```
Redis connected successfully with Railway-compatible settings
Dramatiq broker set successfully!
Worker process is ready for action.
```

**Должно исчезнуть:**
```
Consumer encountered a connection error: invalid username-password pair or user is disabled.
Restarting consumer in 3.00 seconds.
```

### 2. Проверь логи Bot Service

Railway Dashboard → Bot service → Logs

**Должно появиться:**
```
Redis connected successfully with Railway-compatible settings
```

**НЕ должно быть:**
```
Failed to connect to Redis: ...
Redis caching disabled - bot will work with degraded performance
```

### 3. Протестируй CSV обработку

1. Открой бота в Telegram
2. Перейди в "📊 Аналитика портфеля"
3. Загрузи тестовый CSV файл
4. Ответь на вопросы (тип контента, период)
5. **Ожидаемый результат:**
   - Сообщение: "⏳ Обработка запущена! Мы обрабатываем твой файл..."
   - Через 1-2 минуты: "✅ Анализ завершен!" + полный HTML отчёт

### 4. Проверь логи Worker Service (CSV обработка)

Должны появиться логи обработки:
```
🔄 Начинаем обработку CSV анализа <analysis_id>
📊 Обрабатываем файл: uploads/<filename>.csv
✅ CSV обработан: <N> продаж, $<amount>
✅ Результаты сохранены в базу данных
✅ Отчет отправлен пользователю
```

---

## 🔴 Если проблема остаётся

### Вариант A: Проверь Railway Redis credentials

1. Railway Dashboard → Redis service → **Variables**
2. Найди `REDIS_URL` (public URL)
3. Скопируй и замени в **Shared Variables**:
   ```
   REDIS_URL=redis://default:<PASSWORD>@redis-production-XXXX.railway.app:6379
   ```
4. Restart bot + worker services

### Вариант B: Используй Upstash Redis (бесплатный, надёжный)

См. подробную инструкцию: `docs/REDIS_FIX_GUIDE.md` → "Решение 2: Upstash"

**Быстрая инструкция:**
1. https://console.upstash.com → Create Database
2. Region: **EU-West-1** (ближайший к Supabase)
3. Скопируй **Redis URL**
4. Railway → Shared Variables:
   ```
   REDIS_URL=rediss://default:<PASSWORD>@eu1-<NAME>.upstash.io:6379
   ```
5. Restart services

**Преимущества Upstash:**
- ✅ Бесплатный tier: 10,000 команд/день
- ✅ Стабильное подключение (без `invalid username-password`)
- ✅ TLS encryption
- ✅ Низкая латентность из Railway EU

---

## 📊 Ожидаемые улучшения после исправления

| Метрика | До исправления | После исправления | Улучшение |
|---------|----------------|-------------------|-----------|
| **Скорость обработки запросов** | ~1200ms | ~600-800ms | **↓ 40%** |
| **Dramatiq worker** | ❌ Не работает | ✅ Работает | **100%** |
| **User cache** | ❌ Каждый запрос → Supabase | ✅ Из Redis (~5ms) | **↓ 90%** |
| **Lexicon загрузка** | ❌ Каждый запрос (~200ms) | ✅ 1 раз при старте | **↓ 100%** |
| **Supabase connections** | 🟡 Высокая нагрузка | ✅ Меньше запросов | **↓ 50%** |

---

## 🎉 Итоговый чек-лист

После деплоя проверь:

- [ ] Worker service логи: `Redis connected successfully`
- [ ] Bot service логи: `Redis connected successfully`
- [ ] Worker service логи: НЕТ `invalid username-password pair`
- [ ] Тест CSV обработки: отчёт приходит автоматически
- [ ] Worker service логи: видны логи обработки CSV
- [ ] Bot отвечает быстрее (< 1 секунды на простые запросы)
- [ ] Railway Metrics: RAM usage стабильный

---

## 💡 Дополнительные рекомендации

### 1. Мониторинг после деплоя (первые 24 часа)

- Проверяй Railway Metrics каждые 2-3 часа:
  - **RAM usage**: должно быть < 400MB на каждом сервисе
  - **CPU usage**: должно быть < 50% в среднем
  
- Проверяй Supabase Dashboard:
  - **Active Connections**: должно быть < 50 connections

### 2. Load Testing (через 1-2 дня)

Используй `tests/load_test.py` для симуляции нагрузки:
```bash
poetry run python tests/load_test.py --users 50 --duration 300
```

Проверь:
- ✅ Бот отвечает всем пользователям
- ✅ RAM usage < 450MB
- ✅ Нет OOM Kill
- ✅ Нет ошибок в логах

### 3. Готовность к промо-кампании

**Для 1000-2000 пользователей:**
- ✅ Redis должен работать стабильно
- ✅ Load test с 100 пользователями должен пройти успешно
- 🟡 Возможно, потребуется upgrade Railway plan (Pro: $20/month)

---

**Дата создания**: 13.11.2025  
**Версия**: 1.0  
**Статус**: Готово к деплою

