# 📋 Сводка исправлений: Загрузка CSV

## ✅ Исправленные ошибки

### 1. ❌ Ошибка форматирования сообщений об ошибках

**Проблема:**
```
Ошибка при загрузке файла: {error}
```

**Исправление:**
- Добавлено форматирование строки с `.format(error=str(e))`
- Добавлено логирование ошибок с полным traceback
- Файл: `bot/handlers/analytics.py`

---

### 2. ❌ Отсутствие переменных окружения Supabase

**Проблема:**
```
ValueError: Missing required environment variables: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
```

**Исправление:**
- Улучшена логика получения переменных (приоритет: env → settings)
- Улучшены сообщения об ошибках с указанием недостающих переменных
- Файл: `services/storage_service.py`

**📖 Инструкция:** См. `docs/RAILWAY_SUPABASE_VARS_SETUP.md`

---

### 3. ❌ Ошибка скачивания файлов из Telegram

**Проблема:**
```
aiogram.exceptions.TelegramBadRequest: wrong file_id or the file is temporarily unavailable
```

**Исправление:**
- Использование `bot.download(file_info)` вместо `bot.download(file_info.file_path)`
- В aiogram 3.x нужно передавать File объект, а не file_path
- Файл: `bot/handlers/analytics.py`

---

## 📝 Что нужно сделать

### Шаг 1: Commit и Push

```bash
git add bot/handlers/analytics.py services/storage_service.py docs/
git commit -m "fix: CSV upload errors (formatting, env vars, Telegram API)"
git push origin chore/high-load-audit
```

### Шаг 2: Настроить Supabase переменные в Railway

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Без этого загрузка CSV не будет работать!

Railway Dashboard → Settings → Variables (или Shared Variables):

```bash
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Где найти:**
- Supabase Dashboard → Settings → API
- Project URL → `SUPABASE_URL`
- service_role key → `SUPABASE_SERVICE_ROLE_KEY` (НЕ anon key!)

**📖 Подробная инструкция:** `docs/RAILWAY_SUPABASE_VARS_SETUP.md`

### Шаг 3: Railway перезапустит сервисы

После push и добавления переменных Railway автоматически перезапустит все сервисы.

### Шаг 4: Проверить логи

Railway Dashboard → Bot service → Logs

**Должно появиться:**
```
StorageService initialized with bucket: csv-files
```

**НЕ должно быть:**
```
Missing required environment variables: ...
wrong file_id or the file is temporarily unavailable
Ошибка при загрузке файла: {error}
```

### Шаг 5: Протестировать

1. Открой бота в Telegram
2. Перейди в "📊 Аналитика портфеля"
3. Загрузи тестовый CSV файл
4. **Ожидаемый результат:**
   - ✅ Файл принимается
   - ✅ Появляется сообщение "Файл получен: test.csv (50.5 KB)"
   - ✅ Задаётся вопрос о размере портфеля
   - ✅ После ответов: "⏳ Обработка запущена! Мы уведомим тебя..."
   - ✅ Через 1-2 минуты приходит полный отчет

---

## 🔍 Диагностика проблем

### Если ошибка "Missing required environment variables"

1. Проверь, что в Railway установлены:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY` (или `SUPABASE_KEY`)
2. Проверь, что Railway перезапустил сервисы после добавления переменных
3. Проверь логи: должно быть `StorageService initialized with bucket: csv-files`

### Если ошибка "wrong file_id or the file is temporarily unavailable"

1. Убедись, что код использует `bot.download(file_info)`, а не `bot.download(file_info.file_path)`
2. Убедись, что изменения были запушены в Git
3. Проверь, что Railway задеплоил последний commit

### Если ошибка "Ошибка при загрузке файла: {error}"

1. Убедись, что код использует `.format(error=str(e))`
2. Убедись, что изменения были запушены в Git
3. Проверь логи Railway — там должна быть реальная ошибка с traceback

### Если файл не загружается в Supabase Storage

1. Проверь Supabase Storage Policy (см. `docs/SUPABASE_POLICY_FIX.md`)
2. Убедись, что bucket `csv-files` существует
3. Убедись, что используется `SUPABASE_SERVICE_ROLE_KEY`, а не anon key
4. Проверь логи: `Failed to upload CSV to Storage: ...`

---

## 📊 Чек-лист

- [ ] Commit и push изменений в Git
- [ ] Добавлены переменные Supabase в Railway Variables
- [ ] Railway перезапустил все сервисы
- [ ] В логах Bot service: `StorageService initialized with bucket: csv-files`
- [ ] Нет ошибок в логах: `Missing required environment variables`
- [ ] Нет ошибок в логах: `wrong file_id or the file is temporarily unavailable`
- [ ] Нет ошибок в логах: `Ошибка при загрузке файла: {error}`
- [ ] Тест загрузки CSV прошёл успешно
- [ ] Отчёт приходит пользователю через 1-2 минуты

---

## 📚 Дополнительные материалы

- `docs/RAILWAY_SUPABASE_VARS_SETUP.md` — Настройка Supabase переменных
- `docs/CSV_UPLOAD_ERROR_FIX.md` — Детали исправления форматирования ошибок
- `docs/TELEGRAM_DOWNLOAD_FIX.md` — Детали исправления Telegram API
- `docs/SUPABASE_POLICY_FIX.md` — Настройка Supabase Storage Policy

---

**Дата создания**: 13.11.2025  
**Версия**: 1.0  
**Статус**: Готово к деплою

