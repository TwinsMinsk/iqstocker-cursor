# IQStocker Bot

Telegram-бот для аналитики портфолио авторов на стоковых площадках.

## Описание

IQStocker — это Telegram-бот, предназначенный для авторов на стоковых площадках (например, Adobe Stock). Бот предоставляет инструменты для анализа портфеля, генерации тем для съемок, отслеживания трендов и образовательные материалы для увеличения продаж.

## Основные функции

### 📊 Аналитика портфеля
- **Расширенный анализ CSV-файлов** с продажами и метриками
- **AI-прогнозирование продаж** на следующий месяц
- **Advanced метрики:** ROI по темам, conversion rates, portfolio diversity
- **Benchmark сравнения** с индустриальными стандартами
- **Персонализированные инсайты** на основе поведения пользователя
- **Определение новых работ** по ID (порог настраивается через `APP_NEW_WORKS_ID_PREFIX`)

### 🎯 AI-генерация тем
- **Умные рекомендации тем** на основе успешных работ пользователя
- **Анализ глобальных трендов** рынка стоковой фотографии
- **Сезонный анализ** популярных тем по месяцам
- **Персонализированные объяснения** для каждой рекомендации
- **Прогнозируемая производительность** тем
- **Двухшаговая навигация** в разделе "Темы и тренды"
- **Еженедельные уведомления** о доступности новых тем
- **Система кулдауна** с автоматическими напоминаниями
- **История тем** с сохранением всех полученных рекомендаций

### 📈 Расширенная аналитика
- **Топ тем по продажам и доходу** с детальным анализом
- **Growth trend analysis** для выявления паттернов развития
- **Time-to-sale метрики** и revenue per upload
- **Portfolio diversity index** для оценки разнообразия
- **Benchmark ranking** - позиция среди других пользователей

### 🎓 Образовательный контент
- **Видеоуроки** с трекингом просмотров и ограничениями по тарифам
- **Календарь стокера** с AI-генерацией и ручным редактированием
- **Персонализированные рекомендации** контента
- **Раздел "Уроки"** с дифференцированным контентом по тарифам
- **Динамические клавиатуры** с кнопками апгрейда для FREE пользователей

### 💳 Система подписок
- **FREE, TEST_PRO, PRO, ULTRA** с различными лимитами
- **AI-powered рекомендации** для каждого типа подписки
- **Benchmark сравнения** по типам подписки
- **Интеграция с Tribute** для обработки платежей
- **Автоматическое управление лимитами** при смене/продлении подписки

### 🎁 Реферальная система
- **Начисление IQ Баллов** за приглашенных пользователей
- **Система наград:** скидки, бесплатные подписки, ссылки
- **Автоматическое начисление** при первой оплате PRO/ULTRA
- **Управление наградами** через админ-панель

### ⚙️ Админ-панель
- **FastAPI админка** с кастомными представлениями (директория `admin_panel/`)
- **Управление пользователями** и подписками
- **Управление календарем:** создание, редактирование, удаление записей
- **Управление видеоуроками** и контентом
- **Система рассылок** с фильтрацией по подпискам
- **Управление тарифными лимитами** через интерфейс
- **Управление реферальными наградами**
- **Управление лексиконом** (тексты сообщений и кнопок)
- **AI Performance мониторинг** и управление
- **Расширенная аналитика** для администраторов
- **Cache и Rate Limiting управление**

## Технологический стек

### 🤖 AI & Machine Learning
- **OpenAI GPT-3.5-turbo / GPT-4** для анализа и категоризации тем
- **Anthropic Claude** (резервный AI провайдер)
- **Google Gemini** (резервный AI провайдер)
- **scikit-learn** для машинного обучения и прогнозирования
- **pandas & numpy** для анализа данных
- **Система управления LLM провайдерами** с шифрованием API ключей в БД

### 🗄️ База данных
- **PostgreSQL** (production) с оптимизированными индексами
- **SQLite** (development) для локальной разработки
- **Redis** для кэширования AI результатов и rate limiting
- **SQLAlchemy 2.0** ORM с Alembic миграциями
- **Асинхронные сессии** для работы с БД

### 🚀 Backend & API
- **Python 3.11+** с aiogram 3.13.1 для Telegram бота
- **FastAPI** для админ-панели и API endpoints
- **Uvicorn** ASGI сервер для production
- **Flask** для health check endpoint

### 📊 Аналитика & Мониторинг
- **Plotly** для интерактивных графиков и диаграмм
- **ydata-profiling** для расширенного анализа данных
- **Custom AI Performance Monitor** для отслеживания производительности
- **Health checks** для мониторинга состояния системы
- **Structlog** для структурированного логирования

### 🔧 Infrastructure
- **Docker** для контейнеризации
- **Railway** (основной deployment; мультисервис: bot/web/worker)
- **GitHub Actions** для CI/CD (опционально)

### 💳 Платежи
- **Tribute** для обработки платежей и подписок
- **Webhook обработка** через FastAPI
- **Автоматическая активация подписок** при успешной оплате

### 📅 Планировщик задач
- **APScheduler** для фоновых задач
- **Автоматическая генерация календаря** (25 числа каждого месяца)
- **Еженедельные уведомления** о доступности новых тем
- **Уведомления о тарифах** и истечении подписок

## Структура проекта

```
IQStocker-cursor2/
├── env.example             # Пример конфигурации
├── requirements.txt        # Зависимости Python
├── docker-compose.yml      # Docker конфигурация (локально/прод)
├── main.py                 # Точка входа (делегирует в bot/main.py)
├── start_bot.py            # Скрипт запуска бота с настройками
├── README.md               # Документация
│
├── bot/                    # 🤖 Telegram-бот (aiogram 3)
│   ├── main.py             # Основной цикл бота
│   ├── handlers/           # Обработчики команд и callback
│   │   ├── start.py        # Обработчик команды /start
│   │   ├── menu.py         # Главное меню
│   │   ├── profile.py      # Профиль пользователя
│   │   ├── analytics.py    # Аналитика CSV
│   │   ├── themes.py       # Генерация тем
│   │   ├── lessons.py      # Видеоуроки
│   │   ├── calendar.py     # Календарь стокера
│   │   ├── faq.py          # FAQ
│   │   ├── channel.py      # Канал
│   │   ├── payments.py     # Платежи
│   │   ├── admin.py        # Админ-команды
│   │   ├── invite.py       # Приглашения
│   │   └── referral.py     # Реферальная система
│   ├── keyboards/          # Клавиатуры
│   ├── lexicon/            # Тексты и словари (русский)
│   │   └── lexicon_ru.py   # Все тексты сообщений и кнопок
│   ├── middlewares/        # Middleware (DB, подписки, лимиты)
│   │   ├── database.py     # Инъекция DB сессии
│   │   ├── subscription.py # Проверка подписок
│   │   └── limits.py       # Проверка лимитов
│   ├── states/             # FSM состояния
│   └── utils/              # Утилиты
│
├── core/                   # 🧠 Бизнес-логика и сервисы
│   ├── ai/                 # AI компоненты
│   │   ├── llm_service.py   # Фабрика LLM провайдеров
│   │   ├── providers/      # Провайдеры AI (OpenAI, Claude, Gemini)
│   │   ├── theme_manager.py # Генерация тем
│   │   ├── categorizer.py  # Категоризация тем
│   │   ├── cache_manager.py # Кэширование AI запросов
│   │   └── rate_limiter.py  # Rate limiting для AI
│   ├── analytics/          # Аналитика CSV и метрики
│   │   ├── csv_parser.py   # Парсинг CSV файлов
│   │   ├── advanced_csv_processor.py # Расширенная обработка
│   │   ├── metrics_calculator.py # Расчет метрик
│   │   └── report_generator.py # Генерация отчетов
│   ├── notifications/      # Планировщик/уведомления
│   │   ├── scheduler.py    # APScheduler планировщик
│   │   ├── notification_manager.py # Менеджер уведомлений
│   │   └── sender.py       # Отправка уведомлений
│   ├── subscriptions/      # Подписки и лимиты
│   │   ├── payment_handler.py # Обработка платежей
│   │   └── referral_manager.py # Реферальная система
│   ├── tariffs/            # Тарифные планы
│   │   └── tariff_service.py # Сервис управления тарифами
│   ├── admin/              # Админские функции
│   │   ├── calendar_manager.py # Управление календарем
│   │   └── broadcast_manager.py # Рассылки
│   ├── parser/             # Парсеры
│   │   └── adobe_stock.py  # Парсер Adobe Stock
│   ├── monitoring/         # Мониторинг/логирование
│   │   └── ai_monitor.py   # Мониторинг AI производительности
│   └── utils/              # Утилиты
│       └── lexicon_validator.py # Валидация лексикона
│
├── admin_panel/            # 🎛️ FastAPI админ-панель
│   ├── main.py             # FastAPI приложение
│   ├── auth.py             # Аутентификация
│   ├── views/              # Представления
│   │   ├── dashboard.py    # Главная страница
│   │   ├── users.py        # Управление пользователями
│   │   ├── analytics.py    # Аналитика
│   │   ├── broadcast.py    # Рассылки
│   │   ├── payments.py     # Платежи
│   │   ├── tariff_limits.py # Тарифные лимиты
│   │   ├── referral.py     # Реферальная система
│   │   ├── themes.py       # Темы
│   │   ├── lexicon.py      # Управление лексиконом
│   │   └── placeholders.py # Заглушки для несуществующих страниц
│   ├── templates/          # Jinja2 шаблоны
│   └── static/             # Статика (CSS, JS)
│
├── api/                    # 🌐 Служебные API
│   ├── healthcheck.py      # Health check (Flask)
│   ├── webhook_server.py   # FastAPI роутер для webhooks
│   └── webhooks/           # Обработчики webhooks
│       └── tribute.py      # Webhook для платежей Tribute
│
├── database/               # 🗄️ База данных
│   ├── models/             # SQLAlchemy модели
│   │   ├── user.py         # Пользователи
│   │   ├── subscription.py # Подписки
│   │   ├── limits.py       # Лимиты пользователей
│   │   ├── tariff_limits.py # Тарифные лимиты
│   │   ├── csv_analysis.py # Анализ CSV
│   │   ├── theme_request.py # Запросы тем
│   │   ├── user_issued_theme.py # Выданные темы
│   │   ├── global_theme.py # Глобальные темы
│   │   ├── video_lesson.py # Видеоуроки
│   │   ├── calendar_entry.py # Календарь
│   │   ├── referral_reward.py # Реферальные награды
│   │   ├── llm_settings.py # Настройки LLM провайдеров
│   │   ├── lexicon_entry.py # Записи лексикона
│   │   └── system_settings.py # Системные настройки
│   ├── migrations/         # Alembic миграции
│   └── repositories/       # Репозитории (если используются)
│
├── config/                 # ⚙️ Конфигурация
│   ├── settings.py         # Настройки приложения
│   ├── database.py          # Конфигурация БД
│   └── ai.py               # Конфигурация AI (если есть)
│
├── scripts/                # 🔧 Скрипты и утилиты
│   ├── deployment/         # Запуск на Railway (bot/web/worker)
│   │   ├── start_bot.py    # Запуск бота
│   │   ├── start_web.py    # Запуск админ-панели
│   │   ├── start_worker.py # Запуск воркера
│   │   └── run_migrations.py # Применение миграций
│   ├── database/           # Миграции/инициализация
│   ├── data/               # Инициализация данных
│   └── runners/            # Прочие раннеры
│
├── tests/                  # 🧪 Тесты (unit/integration)
├── logs/                   # 📝 Логи
└── uploads/                # 📁 Загруженные файлы
```

## Установка и запуск

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd IQStocker-cursor2
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
# Linux/Mac
source venv/bin/activate
# Windows
venv\Scripts\activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp env.example .env
# Отредактируйте .env файл с вашими настройками
```

5. **Инициализируйте базу данных:**
```bash
python scripts/deployment/run_migrations.py  # применит alembic upgrade head
```

6. **Создайте админского пользователя (опционально):**
```bash
python create_admin_user.py
```

7. **Запуск бота (polling):**
```bash
# Предпочтительно (загружает .env, настраивает пути)
python start_bot.py

# Альтернативно
python main.py
```

8. **Запуск админ-панели (FastAPI):**
```bash
uvicorn admin_panel.main:app --host 0.0.0.0 --port 8000 --reload
```

9. **Запуск воркера (для фоновых задач):**
```bash
python -m dramatiq workers.actors --path . --processes 1 --threads 4
```

### Развертывание на Railway

Проект поддерживает мультисервисный деплой (bot/web/worker). Подробно см. `RAILWAY_DEPLOYMENT_GUIDE.md`.

Кратко:
1. Подключите репозиторий к Railway
2. Создайте 3 сервиса: `bot`, `web`, `worker`
3. Настройте переменные окружения (см. `env.example` и раздел ниже)
4. Команды запуска:
   - bot: `python scripts/deployment/start_bot.py`
   - web: `python scripts/deployment/start_web.py`
   - worker: `python scripts/deployment/start_worker.py`

### Docker

1. **Запустите с Docker Compose:**
```bash
docker-compose up -d
```

## Переменные окружения

### Обязательные переменные

- `BOT_TOKEN` - токен Telegram бота от @BotFather
- `DATABASE_URL` - URL PostgreSQL базы данных (например: `postgresql://user:password@localhost:5432/iqstocker`)
- `REDIS_URL` - URL Redis (например: `redis://localhost:6379/0`)
- `ADMIN_SECRET_KEY` - секретный ключ для админ-панели (сгенерируйте случайную строку)
- `ADMIN_USERNAME` - имя пользователя админ-панели
- `ADMIN_PASSWORD` - пароль админ-панели

### Опциональные переменные

#### AI провайдеры (нужен хотя бы один)
- `OPENAI_API_KEY` - ключ OpenAI API
- `ANTHROPIC_API_KEY` - ключ Anthropic API
- `GEMINI_API_KEY` - ключ Google Gemini API

#### Платежи (Tribute)
- `PAYMENT_TRIBUTE_API_KEY` - API ключ Tribute (используется для проверки подписи webhook)
- `PAYMENT_TRIBUTE_PRO_LINK` - Ссылка на PRO подписку в Tribute
- `PAYMENT_TRIBUTE_ULTRA_LINK` - Ссылка на ULTRA подписку в Tribute
- `PAYMENT_WEBHOOK_URL` - Полный URL для webhook от Tribute (например: `https://your-project.up.railway.app/webhook/tribute`)

#### Шифрование
- `ENCRYPTION_KEY` - ключ для шифрования API ключей в БД (сгенерируйте: `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`)

#### Логирование и мониторинг
- `SENTRY_DSN` - DSN для Sentry (опционально)
- `LOG_LEVEL` - уровень логирования (INFO, DEBUG, WARNING, ERROR)

#### Настройки приложения
- `DEBUG` - режим отладки (True/False)
- `HOST` - хост для админ-панели (по умолчанию: 0.0.0.0)
- `PORT` - порт для админ-панели (по умолчанию: 8000)
- `UPLOAD_FOLDER` - папка для загруженных файлов (по умолчанию: uploads)
- `MAX_FILE_SIZE` - максимальный размер файла в байтах (по умолчанию: 10485760 = 10MB)

#### Тарифные лимиты (можно настроить через админ-панель)
- `APP_FREE_ANALYTICS_LIMIT` - лимит аналитик для FREE (по умолчанию: 0)
- `APP_FREE_THEMES_LIMIT` - лимит тем для FREE (по умолчанию: 4)
- `APP_TEST_PRO_ANALYTICS_LIMIT` - лимит аналитик для TEST_PRO (по умолчанию: 1)
- `APP_TEST_PRO_THEMES_LIMIT` - лимит тем для TEST_PRO (по умолчанию: 2)
- `APP_PRO_ANALYTICS_LIMIT` - лимит аналитик для PRO (по умолчанию: 1)
- `APP_PRO_THEMES_LIMIT` - лимит тем для PRO (по умолчанию: 4)
- `APP_ULTRA_ANALYTICS_LIMIT` - лимит аналитик для ULTRA (по умолчанию: 2)
- `APP_ULTRA_THEMES_LIMIT` - лимит тем для ULTRA (по умолчанию: 4)
- `APP_TEST_PRO_DURATION_DAYS` - длительность TEST_PRO в днях (по умолчанию: 14)

#### Другие настройки
- `APP_NEW_WORKS_ID_PREFIX` - порог ID для определения новых работ (по умолчанию: "1490000000")
- `ADOBE_STOCK_RATE_LIMIT` - лимит запросов к Adobe Stock в секунду (по умолчанию: 10)
- `REDIS_CACHE_TTL` - время жизни кэша в секундах (по умолчанию: 2592000 = 30 дней)

## Подписки и лимиты

### FREE
- **Аналитика:** 0 в месяц
- **Темы:** 4 генерации в месяц
- **Кулдаун тем:** 7 дней
- **Календарь:** базовый
- **Уроки:** базовые видеоуроки

### TEST_PRO (14 дней)
- **Аналитика:** 1 за период (14 дней)
- **Темы:** 2 генерации за период (14 дней)
- **Кулдаун тем:** 7 дней
- **Календарь:** расширенный
- **Уроки:** все видеоуроки

### PRO
- **Аналитика:** 1 в месяц
- **Темы:** 4 генерации в месяц
- **Кулдаун тем:** 7 дней
- **Календарь:** расширенный
- **Уроки:** все видеоуроки

### ULTRA
- **Аналитика:** 2 в месяц
- **Темы:** 4 генерации в месяц
- **Кулдаун тем:** 7 дней
- **Календарь:** расширенный
- **Уроки:** все видеоуроки

### Логика работы лимитов

1. **Регистрация:** Новый пользователь получает TEST_PRO на 14 дней с начальными лимитами
2. **Использование:** Лимиты списываются ПОСЛЕ успешной обработки (аналитика/темы)
3. **Продление подписки:** Лимиты добавляются к существующим (накапливаются)
4. **Смена тарифа:** Лимиты сгорают, начинается новый отсчет
5. **Истечение подписки:** Автоматический переход на FREE, лимиты обнуляются

## Админ-панель

Админ-панель доступна по адресу `/dashboard` (локально: `http://localhost:8000/dashboard`).

### Функции админ-панели

- **Dashboard:** Главная страница с общей статистикой
- **Пользователи:** Управление пользователями, подписками и лимитами
- **Аналитика:** Расширенная аналитика по пользователям и активности
- **Рассылки:** Массовые уведомления с фильтрацией по подпискам
- **Платежи:** Просмотр истории платежей и подписок
- **Тарифные лимиты:** Управление лимитами для каждого тарифа
- **Реферальная система:** Управление наградами и просмотр статистики
- **Темы:** Управление глобальными темами
- **Лексикон:** Управление текстами сообщений и кнопок
- **Календарь:** Создание, редактирование и удаление календарных записей

### Аутентификация

Админ-панель использует сессионную аутентификацию. Войдите используя `ADMIN_USERNAME` и `ADMIN_PASSWORD` из переменных окружения.

## API Endpoints

### Health Check
- `GET /health` - проверка состояния сервиса (Flask, см. `api/healthcheck.py`)

### Webhooks
- `POST /webhook/tribute` - обработка платежей от Tribute (FastAPI, см. `api/webhook_server.py`)
- `GET /webhook/tribute` - проверка доступности webhook endpoint
- `POST /webhook/tribute/test` - тестовый endpoint для симуляции webhook без реальной оплаты

## Реферальная система

### Как работает

1. **Приглашение:** Пользователь получает уникальную реферальную ссылку
2. **Регистрация:** Новый пользователь регистрируется по ссылке
3. **Начисление баллов:** При первой оплате PRO/ULTRA приглашенным пользователем, рефереру начисляется 1 IQ Балл
4. **Обмен баллов:** Пользователь может обменять баллы на награды:
   - Скидки (25% или 50%)
   - Бесплатная подписка PRO (1 месяц)
   - Бесплатная подписка ULTRA (1 месяц)
   - Ссылки на внешние сервисы (например, IQ Radar)

### Управление наградами

Награды настраиваются через админ-панель в разделе "Реферальная система". Можно создавать, редактировать и удалять награды.

## Система календаря

### AI-генерация

Календарь автоматически генерируется 25 числа каждого месяца с помощью OpenAI GPT-4. Генерация включает:
- Описание сезона (2-3 предложения)
- Темы для загрузки сейчас (4-5 тем)
- Темы для подготовки (4-5 тем, актуальны через 1-2 месяца)

### Ручное редактирование

Администратор может:
- Создавать новые календарные записи
- Редактировать существующие записи
- Удалять записи
- Использовать шаблоны вместо AI-генерации

### Отображение в боте

Пользователи видят календарь в зависимости от тарифа:
- **FREE:** Базовый календарь
- **TEST_PRO/PRO:** Расширенный календарь
- **ULTRA:** Расширенный календарь с дополнительными функциями

## Система лексикона

Все тексты сообщений, названия кнопок и другие строки хранятся в `bot/lexicon/lexicon_ru.py` и в базе данных (таблица `lexicon_entries`).

### Правила работы с лексиконом

1. **НИКОГДА не хардкодить** русский текст в handlers или services
2. **Всегда использовать** `LEXICON_RU` для сообщений и `LEXICON_COMMANDS_RU` для кнопок
3. **Добавлять новые тексты** в лексикон, следуя существующей структуре
4. **Управлять лексиконом** через админ-панель (раздел "Лексикон")

### Валидация

При запуске бота выполняется валидация лексикона - проверяется, что все необходимые ключи присутствуют в словаре.

## Разработка

### Запуск тестов
```bash
pytest
```

### Форматирование кода
```bash
black .
isort .
```

### Линтинг
```bash
flake8
```

### Структура логов

Логи сохраняются в директории `logs/`:
- `logs/bot.log` - логи бота
- Логи админ-панели выводятся в консоль

## Мониторинг

- **Логи:** директория `logs/`
- **Админ-панель локально:** `http://localhost:8000/dashboard`
- **Health check:** `GET /health` (см. `api/healthcheck.py`)
- **AI Performance Monitor:** отслеживание производительности AI запросов

## Особенности реализации

### Middleware

Бот использует три уровня middleware (порядок важен):
1. **DatabaseMiddleware** - инъекция DB сессии в handlers
2. **SubscriptionMiddleware** - проверка подписок и доступности функций
3. **LimitsMiddleware** - проверка лимитов перед выполнением операций

### Планировщик задач

Используется APScheduler для:
- Автоматической генерации календаря (25 числа каждого месяца)
- Еженедельных уведомлений о доступности новых тем
- Уведомлений об истечении подписок
- Других периодических задач

### Кэширование

- **Redis** используется для кэширования:
  - AI результатов (TTL: 30 дней)
  - Rate limiting данных
  - Других временных данных

### Шифрование API ключей

API ключи AI провайдеров хранятся в БД в зашифрованном виде. Используется Fernet (symmetric encryption) с ключом из `ENCRYPTION_KEY`.

## Лицензия

MIT License

## Поддержка

Для получения поддержки обращайтесь:
- Email: [email]
- Telegram: [@ник]
